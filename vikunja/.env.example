# Copy to .env next to docker-compose.yml:
#   cp .env.example .env
# Compose loads this file via env_file in docker-compose.yml.
# Set a strong random value, e.g. openssl rand -hex 32
VIKUNJA_SERVICE_JWTSECRET=
